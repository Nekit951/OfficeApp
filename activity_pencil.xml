<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    tools:context=".PencilActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_pencil"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_marginTop="30dp">

        <TextView
            android:id="@+id/toolbar_title_pencil"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Карандаши"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:layout_gravity="center_horizontal"/>

        <ImageView
            android:id="@+id/vector_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:contentDescription="Вернуться"
            android:src="@drawable/vector_back" />


    </androidx.appcompat.widget.Toolbar>

    <!-- RecyclerView для отображения списка карандашей -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/pencilList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar_pencil"
        android:scrollbars="vertical" />

</RelativeLayout>
