package com.example.officeapp

import android.content.Intent
import android.os.Bundle
import android.util.Log
import android.widget.ImageView
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
import androidx.recyclerview.widget.LinearLayoutManager
import androidx.recyclerview.widget.RecyclerView

class PenActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        Log.d("DEBUG", "PenActivity.onCreate() вызван")
        enableEdgeToEdge()
        setContentView(R.layout.activity_pen)
        Log.d("DEBUG", "Layout установлен: activity_pen")

        val penList: RecyclerView = findViewById(R.id.pensList)
        val pens = arrayListOf<Pen>()

        pens.add(Pen(1, "Ручка 1", 100, 50))
        pens.add(Pen(2, "Ручка 2", 90 ,40))
        pens.add(Pen(3, "Ручка 3", 80, 45))

        penList.layoutManager = LinearLayoutManager(this)
        penList.adapter = PenAdapter(pens, this)

        val vector_back: ImageView = findViewById(R.id.vector_back)
        vector_back.setOnClickListener {
            val intent = Intent(this, StoreActivity::class.java)
            startActivity(intent)
        }
    }
}
